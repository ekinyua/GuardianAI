import 'package:flutter/material.dart';

class SafeWalkScreen extends StatelessWidget {
  const SafeWalkScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Safe Walk');
  }
}
